<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xupt.xiyoumobile.web.dao.IWorkMapper">

    <insert id="insertOpeningReport" parameterType="com.xupt.xiyoumobile.web.entity.OpeningReport">
        INSERT INTO xupt_research_team_work_report_table
        (author, abstract_zh, abstract_en, type)
        VALUES (#{author}, #{abstractCh}, #{abstractEn}, #{type})
    </insert>

    <select id="findReportByUserAccountAndType" resultType="com.xupt.xiyoumobile.web.entity.WorkReport">
        SELECT * FROM xupt_research_team_work_report_table
        WHERE author = #{userAccount} AND type = #{type}
    </select>

    <update id="modifyReportSelective" parameterType="com.xupt.xiyoumobile.web.entity.WorkReport">
        UPDATE xupt_research_team_work_report_table
        <set>
            <if test="author != null">
                author = #{author},
            </if>
            <if test="abstractZh != null">
                abstract_zh = #{abstractZh},
            </if>
            <if test="abstractEn != null">
                abstract_en = #{abstractEn},
            </if>
            <if test="reportUrl != null">
                report_url = #{reportUrl},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="checkValid">
        SELECT COUNT(*) FROM xupt_research_team_work_report_table
        WHERE id = #{id} AND author = #{userAccount}
    </select>

    <delete id="deleteWorkReportByUserAccountAndId">
        DELETE FROM xupt_research_team_work_report_table
        WHERE id = #{id} AND author = #{userAccount}
    </delete>

    <select id="findReportByReportId" resultType="com.xupt.xiyoumobile.web.entity.WorkReport">
        SELECT * FROM xupt_research_team_work_report_table
        WHERE id = #{workReportId}
    </select>
</mapper>